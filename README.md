# K-Means-Clustering-Initialization-Sensitivity-and-Convergence-Analysis
This project presents a from-scratch implementation of the K-Means clustering algorithm and investigates the impact of initialization strategies on convergence behavior and objective function minimization. Two centroid initialization strategies are evaluated across cluster sizes  𝑘 = 2 k=2 to  𝑘 = 10 k=10
